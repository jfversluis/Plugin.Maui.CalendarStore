<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Plugin.Maui.Calendar.Sample.MainPage"
             Title="Calendar Plugin">

    <VerticalStackLayout Spacing="10" Margin="10">
        <Button Text="Load Calendars" Clicked="Button_Clicked_Calendars" />

        <VerticalStackLayout BindableLayout.ItemsSource="{Binding Calendars}">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding Name}" />
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </VerticalStackLayout>

        <Button Text="Load Events" Clicked="Button_Clicked_Events" />

        <VerticalStackLayout BindableLayout.ItemsSource="{Binding Events}">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding Title}" />
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </VerticalStackLayout>
    </VerticalStackLayout>
</ContentPage>
